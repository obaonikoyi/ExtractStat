class WordStatsManager:

    def print_summary(number, output_spec, input_paths):
        for path in input_paths:
            FileAccess fileAcces = new TextFileAccess()
            fileAcces.open_read(path)

    